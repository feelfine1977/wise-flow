# src/bpmn

`importBpmn` (bpmn-moddle → FlowGraph + task mapping), `<BpmnView/>` hosting bpmn-js viewer or modeler with the overlay bridge and selection callbacks for authoring, BPMN-lite rendering in the core model.
